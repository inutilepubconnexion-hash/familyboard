# ─────────────────────────────────────────────
# FAMILYBOARD — DÉPENDANCES PYTHON (requirements.txt)
#
# Ce fichier liste toutes les bibliothèques Python
# nécessaires pour faire fonctionner le backend.
#
# Pour les installer, ouvre un terminal et tape :
#   pip install -r requirements.txt
# ─────────────────────────────────────────────

# Flask : le framework qui crée le serveur web
flask==3.0.3

# Flask-CORS : autorise le site web à parler au serveur
#              même s'ils sont sur des domaines différents
flask-cors==4.0.1

# Gunicorn : serveur de production utilisé par Render
#            (ne sert à rien en local, mais nécessaire pour l'hébergement)
gunicorn==22.0.0
